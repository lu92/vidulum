server:
  port: 0  # Random port for tests

spring:
  application:
    name: vidulum-websocket-gateway-test

  kafka:
    consumer:
      group-id: websocket-gateway-test
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"

gateway:
  jwt:
    secret: dGVzdFNlY3JldEtleUZvckp3dFRva2VuR2VuZXJhdGlvblRoYXRJc0F0TGVhc3QyNTZCaXRzTG9uZyE=
  websocket:
    endpoint: /ws
    allowed-origins: "*"

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    com.multi.vidulum.gateway: DEBUG
    org.springframework.web.socket: DEBUG
